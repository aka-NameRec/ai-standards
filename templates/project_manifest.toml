ai_standards_version = "2.2.1"
project_version = "replace-me"
project_release_date = "YYYY-MM-DD"
fragments = [
  "core/base",
  "core/git-workflow",
  "core/architecture",
  "core/error-handling",
]
features = [
  "conport",
  # "basic-memory",
  # "chroma",
  "design-first-collaboration",
  "reasoning-hygiene",
  "autonomy-boundaries",
  "structured-artifacts",
  "session-hygiene",
  "agent-usage-hygiene",
  "code-review",
]
stacks = [
  "python",
  "fastapi",
]
local_overrides = [
  "ai/project-rules.md",
]
optional_local_overrides = [
  "ai/private-rules.local.md",
]

# Optional. Defaults: knowledge_tree = "docs", dated_note_directories = ["domain", "decisions", "architecture"].
# [basic_memory]
# knowledge_tree = "docs"
# dated_note_directories = ["domain", "decisions", "architecture"]

[tooling]
agents = []

[metadata]
project_name = "replace-me"
notes = "Project-specific AI configuration using manifest-only dependencies."
