ai_standards_version = "2.3.0-2026-09-03"
fragments = [
  "core/base",
  "core/git-workflow",
  "core/architecture",
  "core/error-handling",
]
features = [
  "retrieval-routing",
  # "basic-memory",
  # "project-memory",
  # "chroma",
  "design-first-collaboration",
  "reasoning-hygiene",
  "autonomy-boundaries",
  "structured-artifacts",
  "session-hygiene",
  "agent-usage-hygiene",
  "code-review",
]
stacks = [
  "python",
  "fastapi",
]
local_overrides = [
  "ai/project-rules.md",
]
optional_local_overrides = [
  "ai/private-rules.local.md",
]

# Optional. Defaults: knowledge_tree = "docs", dated_note_directories = ["domain", "decisions", "architecture"].
# [basic_memory]
# knowledge_tree = "docs"
# dated_note_directories = ["domain", "decisions", "architecture"]

# Optional. Local working memory area inside the knowledge tree; enabled by the
# "project-memory" feature. Gitignore it: '/docs/local/' by default.
# [project_memory]
# local_tree = "local"

[tooling]
agents = []

[metadata]
project_name = "replace-me"
notes = "Project-specific AI configuration using manifest-only dependencies."
