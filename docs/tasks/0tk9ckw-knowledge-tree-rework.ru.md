---
title: 0tk9ckw — Переработка дерева знаний и аудит
permalink: ai-standards/tasks/0tk9ckw-knowledge-tree-rework.ru
---

# 0tk9ckw — Переработка дерева знаний и аудит

Задача: `0tk9ckw` (зонтичная; подзадачи 0tk6h6b, 0tk6h8x, 0tk7xg0, 0tk85o3, 0tk868q, 0tk9bdz, 0tk9ckw, 0tk9d9t, 0tk9qdl, 0tk9qeh, 0tk9qqb, 0tk9t0y, 0tk9t95) · Ветка: `feature/0tk9ckw-archive-history-in-docs-tree` · 2026-08-22 — 2026-08-27

Английская версия: [0tk9ckw-knowledge-tree-rework.md](0tk9ckw-knowledge-tree-rework.md)

## Что сделано

**Линия pull request-ов (ai-standards):** ревью #6 (фича code-review), #7 (дерево знаний), #8 (проход DRY), #9 (унификация отчёта). #6, #8, #9 слиты пользователем; разрешения конфликтов отправлены в форки контрибьюторов через флаг maintainer-can-modify (#8: аддитивный конфликт CHANGELOG; #7: аддитивный конфликт тестов). Живой баг из неотслеживаемого артефакта — HTML-маркеры, ломающие шаблоны `.py`/`.toml` — исправлен пер-форматно, с тестами, и влит в локальный `main` (`5de0bcb`).

**Направление переработки:** pull request #7 не берётся в исходном виде. Переработку задают два решения: [archive-history-in-docs-tree](../decisions/2026-08-24-archive-history-in-docs-tree.ru.md) и [разделение ролей хранилищ](../decisions/2026-08-24-knowledge-store-role-separation.ru.md). #7 влит в эту ветку (`2ab86bf`) как база переработки.

**Документация:** три методических документа со ссылками из README — [жанры знаний](0tk9qoy-knowledge-genres.ru.md), [ai-sync doctor](0tk9qpd-ai-sync-doctor.ru.md), [скилл audit-knowledge-tree](0tk9qpt-audit-knowledge-tree.ru.md) — плюс [обзор стека](../architecture/2026-08-27-knowledge-stack-roles.ru.md) и `MODULE_CONTRACT.md`.

**Аудит применён (0tk9t95):** `ai-sync doctor` нашёл 264 предупреждения / 0 ошибок; `--fix` дал 90 заметкам заголовки frontmatter (пермалинки штампнула работающая синхронизация Basic Memory); сужденческая половина скилла добавила Observations/Relations восьми документам сессии и переименовала один русский H1, устранив коллизию дублей заголовков. Остаток: 158 предупреждений (78 заметок без наблюдений, 80 без связей) — сознательно отложены в периодическую гигиену: синтез наблюдений пачкой запрещён.

**Сторона cockpit:** путь Basic Memory проекта `git-updater` сужен с корня репозитория до `docs` (коммит реестра `dd4be88`; адаптер пересоздал запись проекта; поиск проверен).

## Находки, оставленные гигиене

- ~20 двуязычных пар решений имеют одинаковый H1 в обеих версиях; граф идентифицирует заметки по заголовку, поэтому это дубликаты идентификаторов. Лечится локализацией русского H1, по одному файлу.
- 78 заметок без наблюдений / 80 без связей: заполняются, когда человек работает с документом.
- `docs/ai/project-rules.md` в git-updater лежит внутри индексируемого дерева (скаффолд до #7); пять файлов `-log-` остаются в том дереве.

## Ожидает

- Коммит результата аудита (90 файлов; ожидает одобрения — дерево сознательно не закоммичено).
- Реализация решения об архиве на этой ветке: перенос логов `temp/` в `docs/archive/`, проверка `.bmignore` и пропуск архива в `doctor`, починка глоба `-log-` до `docs/**/*-log-*.md`.
- Одна строка в `~/.basic-memory/.bmignore` (`archive/`), когда появится первый архив.
- Пуш `main` (фикс маркеров) и этой ветки; релиз и перерендер downstream-проектов; миграция devcats на `.ai-standards/` разблокирована фиксом маркеров.

## Observations

- [fact] Артефакт 2026-08-03 о баге маркеров описывал реальный неисправленный дефект; фикс лёг только 2026-08-24, а неотслеживаемые артефакты остаются в одном `git clean` от потери.
- [fact] Разрешения конфликтов можно доставлять в форки контрибьюторов без force-push при включённом флаге maintainer-can-modify: мерж-коммит обновляет pull request на месте.
- [fact] Basic Memory штампует пермалинки при синхронизации, как только у файла появляется frontmatter; `doctor --fix` пишет только заголовки.

## Relations

- relates_to [[РЕШЕНИЕ: archive-history-in-docs-tree]]
- relates_to [[РЕШЕНИЕ: разделение ролей хранилищ знаний]]
- relates_to [[Роли стека знаний]]
- локальная версия [[0tk9ckw — Knowledge Tree Rework and Audit]]